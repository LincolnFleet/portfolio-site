import React, {} from 'react';

import {
} from '../Bin-Components.js';

function temp() {
    return (
        <div className='temp' style={styleObj.temp}>
        </div>
    );
};

export default temp;

// <-- CSS -->

const styleObj={
    'temp':{
    },
};