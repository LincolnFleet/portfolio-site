import React, {} from 'react';

function temp() {
    return (
        <div className='temp' style={styleObj.temp}>
        </div>
    );
};

export default temp;

// <-- CSS -->

const styleObj={
    'temp':{
    },
};